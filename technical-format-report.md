# گزارش فنی — اصلاح اصولی سیستم Technical Formatting (نسخه ۳)

## ۱. مشکل ریشه‌ای نسخه فعلی (V2) چه بود؟

V2 (از Edit ۳) معماری Rule-based درستی داشت (`auto_script` عمومی به‌جای Rule
موردی برای هر نماد)، اما **موتور اجرا** یک ضعف ساختاری داشت: Ruleها به
ترتیب و به‌صورت **متوالی** روی رشته اعمال می‌شدند (`output = output.replace(...)`
برای هر Rule، پشت سر هم). این یعنی هیچ مکانیزمی برای «اولویت بین Ruleها»
وجود نداشت — وقتی دو Rule روی یک بازه از متن هم‌پوشانی داشتند، هرکدام دیرتر
در حلقه اجرا می‌شد برنده می‌شد، نه هرکدام که واقعاً باید برنده باشد.

مشکل دوم: طول و نوع دنباله در `auto_script` (`[A-Za-z0-9]{1,3}`) مستقیماً
در کد ثابت بود، نه در Database — یعنی برای افزودن یک notation جدید با
پروفایل ریسک متفاوت (مثلاً m² که فقط باید رقم بپذیرد، نه حرف)، باز هم
نیاز به تغییر کد بود، دقیقاً همان چیزی که دستور صراحتاً ممنوع کرده بود.

مشکل سوم (که در بررسی این Session کشف شد، نه در دستور اولیه فهرست شده
بود): لیست ادمین محصولات (`/api/store/products` حالت مدیریت) از همان
`applyTechnicalFormattingToProduct` استفاده می‌کرد که لیست عمومی فروشگاه
استفاده می‌کند. چون فرم ویرایش محصول در پنل مدیریت (`editProduct` در
`products.js`) دقیقاً از همین لیست پر می‌شود، مدیر داشت **متن فرمت‌شده**
(با نویسه‌های یونیکد subscript جایگزین‌شده) را ویرایش می‌کرد، نه متن خام —
و با هر Save، آن متن فرمت‌شده به‌جای متن خام در D1 ذخیره می‌شد. این دقیقاً
نقض همان تضمینی بود که در کامنت‌های خودِ V2 نوشته شده بود: «داده خام
محصولات... دست‌نخورده باقی می‌ماند».

## ۲. چرا V2 در عمل «Qm / Pm / °C» را کامل حل نکرد؟

با تست مستقیم منطق V2 (اجرای واقعی تابع با Ruleهای واقعی seed‌شده)،
`Qm`، `Pm`، `°C` به‌تنهایی **درست** کار می‌کردند. مشکل واقعی جای دیگری
بود: وقتی واحدهای بیشتری مثل `Pa` (پاسکال) اضافه می‌شدند، قانون عمومی
`auto_script` با پیشوند `P` بدون اطلاع از وجود واحد `Pa`، آن را هم به
`Pₐ` تبدیل می‌کرد — یعنی مشکل «Qm/Pm ناقص حل شده» در واقع مشکل **مقیاس‌پذیری**
بود: هرچه واحد بیشتری اضافه می‌شد، تصادم با Ruleهای عمومی محتمل‌تر می‌شد،
و V2 هیچ مکانیزمی برای حل این تصادم نداشت.

## ۳. معماری جدید Formatter چیست؟

فایل جدید و مستقل `src/technical-format.js` (تنها منبع منطق فرمت‌دهی در
کل پروژه؛ `src/index.js` دیگر هیچ Regex فنی مستقیم ندارد):

- **جمع‌آوری Candidate**: هر Rule به‌صورت مستقل، همه تطابق‌های خودش را
  روی متن **اصلی** (نه متن درحال‌تغییر) پیدا می‌کند.
- **حل تداخل با اولویت (Tier)**: `exception` (۴) > `token` (۳) > `unit` (۲)
  > `auto_script` (۱). در تصادم، اول Tier بالاتر، بعد طول تطابق بلندتر،
  بعد `sort_order` مدیر. این دقیقاً مشکل `Pa` در برابر `P` را حل می‌کند:
  یک Rule از نوع `unit` برای `Pa` کافی است تا آن بازه را «رزرو» کند و
  مانع اعمال Rule عمومی `P` روی همان بازه شود — بدون هیچ کد اختصاصی.
- **اعمال یک‌باره و غیر تداخلی**: بعد از انتخاب تطابق‌های نهایی (بدون
  هم‌پوشانی)، خروجی در یک عبور ساخته می‌شود — نه با replace متوالی.
- **suffix_mode و max_suffix_len per-rule**: به‌جای الگوی ثابت در کد،
  هر Rule از نوع `auto_script` می‌تواند بگوید دنباله‌اش فقط رقم است
  (`digits`، برای m²/m³) یا حرف+رقم (`alnum`، برای Qn/Qmax/...)، و حداکثر
  چند نویسه بعد از پیشوند مجاز است.
- **HTML-safety واقعی**: `formatTechnicalHtml` متن HTML را با یک
  tokenizer سبک و quote-aware به بخش «tag» و «text» تقسیم می‌کند و
  **فقط** بخش‌های text را فرمت می‌کند؛ `href`، `src`، `class`، و هر
  attribute دیگری، و همچنین نام خودِ تگ‌ها، هرگز لمس نمی‌شوند.

## ۴. Ruleهای Database چه نقشی دارند؟

جدول `technical_format_rules` دقیقاً همان‌طور که دستور خواسته، محدود به
Configuration واقعی است:
- `unit` → تعریف واحد (شامل نمونه‌های identity مثل `bar`→`bar` که فقط
  فاصله‌گذاری را استاندارد می‌کند، و نمونه‌های تبدیلی مثل `C`→`°C`).
- `auto_script` → تعریف یک notation عمومی subscript/superscript (پیشوند
  + نوع دنباله + حداکثر طول) — هرگز یک Rule جدا برای هر ترکیب.
- `token` → alias/جایگزینی ثابت برای مواردی که با ساختار subscript/superscript
  قابل استخراج نیستند.
- `exception` (**نوع جدید**) → استثنای صریح: این رشته دقیقاً همین‌طور
  باقی می‌ماند، مستقل از این‌که چه Rule دیگری ممکن است رویش تطابق داشته
  باشد. Escape hatch عمومی برای هر تصادم آینده‌ای که از طریق اولویت
  Tier به‌تنهایی حل نشود.

منطق عمومی parser/formatter کاملاً در کد (`technical-format.js`) است؛ جدول
هرگز فهرست صدها Rule موردی نمی‌شود.

## ۵. Formatter در چه نقاطی اجرا می‌شود؟

فقط در مسیرهای نمایش عمومی/SSR — دقیقاً همان ۴ نقطه‌ای که قبلاً بود، با
یک اصلاح:

| مسیر | قبل | بعد |
|---|---|---|
| `GET /api/store/products` (عمومی) | فرمت می‌شد | فرمت می‌شود (بدون تغییر) |
| `GET /api/store/products` (پنل مدیریت، لیست/ویرایش) | فرمت می‌شد ← **باگ** | **دیگر فرمت نمی‌شود** (متن خام برای ویرایش) |
| `GET /api/store/products/:slug` (عمومی) | فرمت می‌شد | فرمت می‌شود (بدون تغییر) |
| صفحه SSR محصول | فرمت می‌شد | فرمت می‌شود (بدون تغییر) |

## ۶. چه فایل‌هایی تغییر کرده‌اند؟

فقط `src/index.js` (حذف کامل منطق قدیمی inline، جایگزینی با import از
ماژول جدید + اصلاح باگ لیست ادمین). هیچ فایل دیگری (فروشگاه، Shipping،
Payment، Account، Invoice، UI عمومی، CSS) لمس نشده — تأیید شده با `diff`
مستقیم بین نسخه اصلی و نسخه نهایی.

## ۷. چه فایل‌هایی اضافه شده‌اند؟

- `src/technical-format.js` — کل موتور جدید (تنها منبع منطق).
- `database/technical-format-rules-v3.sql` — Migration جدید.
- `technical-format-report.md` — همین گزارش.

## ۸. آیا Migration جدید لازم است؟

بله، `database/technical-format-rules-v3.sql` — باید روی D1 زنده اجرا
شود (Migrationهای قبلی technical-format-rules دوباره اجرا نشوند، دقیقاً
طبق دستور خودتان که گفتید نسخه‌های قبلی از قبل زنده‌اند):

```
npx wrangler d1 execute tasisatapadana-db --remote --file=./database/technical-format-rules-v3.sql
```

این Migration جدول را بازسازی می‌کند (الگوی استاندارد SQLite برای تغییر
CHECK)، دو ستون جدید اضافه می‌کند، نوع Rule جدید `exception` را مجاز
می‌کند، و ۹ واحد کم‌ریسک (`F`، `bar`، `Pa`، `kPa`، `MPa`، `kW`، `kWh`،
`Hz`، `RPM`) + یک Rule عمومی بالانویس برای `m` (پوشش m²/m³) را seed
می‌کند. تمام Ruleهای قبلی (Q، P، °C) دقیقاً حفظ می‌شوند.

**اجرا شده در این Session**: کل زنجیره Migration (v1 + v2 + v3) واقعاً
روی یک SQLite in-memory synthetic اجرا شد تا مطمئن شویم بدون خطا اعمال
می‌شود و داده قبلی حفظ می‌شود (نتیجه در بخش تست‌ها).

واحدهای تک‌حرفی پرریسک `W`، `V`، `A` عمداً seed نشده‌اند (توضیح در بخش
۱۳). برای افزودن هرکدام در آینده، فقط کافی است ردیف مشابه با همان الگو
INSERT شود — بدون Deploy مجدد کد.

## ۹. تست‌های انجام‌شده و نتیجه هرکدام

همه تست‌ها **واقعاً اجرا شدند** (نه ادعا)، در دو لایه:

**لایه ۱ — واحد (Unit tests) روی موتور خام**، ۵۳ بررسی: تمام نمونه‌های
Subscript دستور (Qn/Qm/Pn/Pm/Qmax/Qmin)، superscript (m2/m3)، واحدها
(°C/°F/bar/Pa/kPa/MPa/kW/kWh/Hz/RPM)، False positives (Quality/Power/Q/P/
اعداد تنها)، Idempotency (اجرای دوباره روی خروجی)، متن فارسی دست‌نخورده،
HTML-safety (href/src/alt/class دست‌نخورده، tag ساختار حفظ‌شده)، و edge
case مربوط به `>` داخل attribute quote‌شده. **نتیجه: ۵۳ از ۵۳ پاس.**

**لایه ۲ — End-to-end با Ruleهای واقعاً Migrate‌شده**، ۵۶ بررسی: همان
مجموعه تست بالا اما این‌بار Ruleها از یک SQLite واقعی (بعد از اجرای واقعی
هر سه فایل Migration پشت‌سرهم) خوانده شدند، به‌علاوه یک محصول کامل و
واقع‌گرایانه (description حاوی HTML واقعی + brand + model + specs) از
ابتدا تا انتها با `applyTechnicalFormattingToProduct` پردازش شد.
**نتیجه: ۵۶ از ۵۶ پاس.**

همچنین: `node --check` روی هر سه فایل تغییریافته/جدید، و یک Import واقعی
کل `src/index.js` (حل کامل زنجیره import تا `technical-format.js` و
`shipping-engine.js`) بدون خطا.

## ۱۰. نمونه Before/After واقعی

از خروجی واقعی تست لایه ۲ (بدون هیچ ویرایش دستی):

```
قبل:
<p>ظرفیت دستگاه <strong>Qmax</strong> بالا است و در فشار <strong>1500 Pa</strong> کار می‌کند.</p>
<a href="/spec/Qm-60C-model">مشاهده مشخصات کامل مدل Qm-60C</a>
<img src="/img/60C-icon.png" alt="60C icon">
<table><tr><td>دبی</td><td>Qm</td></tr><tr><td>دما</td><td>60 C</td></tr></table>

بعد:
<p>ظرفیت دستگاه <strong>Qₘₐₓ</strong> بالا است و در فشار <strong>1500 Pa</strong> کار می‌کند.</p>
<a href="/spec/Qm-60C-model">مشاهده مشخصات کامل مدل Qₘ-60 °C</a>
<img src="/img/60C-icon.png" alt="60C icon">
<table><tr><td>دبی</td><td>Qₘ</td></tr><tr><td>دما</td><td>60 °C</td></tr></table>
```

دقت کنید: `href="/spec/Qm-60C-model"` و `src="/img/60C-icon.png"` و
`alt="60C icon"` **کاملاً دست‌نخورده** مانده‌اند (چون داخل tag/attribute
هستند)، در حالی که همان رشته‌های «Qm» و «60C» وقتی به‌عنوان متن قابل‌مشاهده
(داخل `<a>` یا `<td>`) ظاهر شده‌اند، درست تبدیل شده‌اند. و «1500 Pa» به‌عنوان
واحد Pascal دقیقاً محافظت شده، تبدیل نشده به Pₐ.

## ۱۱. آیا سیستم Idempotent است؟

بله — هم در تست لایه ۱ (۱۴ ورودی) و هم در تست لایه ۲ (روی کل Object
محصول شامل HTML)، اجرای دوباره Formatter روی خروجی قبلی، **دقیقاً همان
خروجی** را داد. این نتیجه ساختاری موتور است، نه یک قانون جداگانه: نویسه‌های
یونیکد subscript/superscript در دسته `[A-Za-z0-9]` نیستند، پس هیچ Rule
دیگری آن‌ها را دوباره نمی‌بیند.

## ۱۲. آیا HTML-safe است؟

بله، با یک tokenizer text/tag که در همین Session با تست واقعی (نه فرضی)
تأیید شد — شامل edge case صریح «`>` داخل مقدار quote‌شده attribute» که
عمداً تست شد تا مطمئن شویم مرز تگ اشتباه تشخیص داده نمی‌شود.

یک نکته صادقانه: به‌جای `HTMLRewriter` (API بومی Cloudflare Workers)، از
یک tokenizer دستی در همین فایل استفاده شد. دلیل: `HTMLRewriter` فقط در
Runtime واقعی Workers قابل اجراست و نیاز به buffering بین چند text-chunk
دارد که بدون یک Deploy واقعی در این Sandbox قابل تأیید با تست واقعی نبود؛
tokenizer دستی همین امروز با Node.js قابل اجرا و تست واقعی بود. اگر در
آینده تصمیم به مهاجرت به `HTMLRewriter` گرفتید، امضای ورودی/خروجی
`formatTechnicalHtml` طوری طراحی شده که این جایگزینی بدون تغییر در
فراخوانی‌کننده‌ها ممکن است.

محدودیت شناخته‌شده (مستند، نه باگ): کامنت‌های HTML حاوی `>` بدون quote
(مثل `<!-- a>b -->`) ممکن است مرز کامنت را زودتر از موعد تشخیص دهند.
محتوای Description در این پروژه از پنل مدیریت و از طریق یک Rich-Text
Toolbar محدود تولید می‌شود که کامنت HTML تولید نمی‌کند، پس این یک ریسک
عملی امروز نیست.

## ۱۳. آیا افزودن notation جدید در آینده بدون تغییر چندین نقطه از کد ممکن است؟

بله، و این دقیقاً همان چیزی است که کل این بازنویسی برایش انجام شد. سه
نمونه واقعی که همین امروز، **فقط با یک INSERT در D1**، بدون Deploy مجدد
کد قابل‌انجام‌اند:

```sql
-- افزودن واحد ولتاژ (اگر و وقتی تصمیم گرفتید ریسکش را بپذیرید)
INSERT INTO technical_format_rules (rule_type, match_value, display_value, sort_order)
VALUES ('unit', 'V', 'V', 30);

-- افزودن cm² (سانتی‌متر مربع) — با همان Rule عمومی m، بدون تغییر موتور
INSERT INTO technical_format_rules (rule_type, match_value, script_type, suffix_mode, max_suffix_len, sort_order)
VALUES ('auto_script', 'cm', 'superscript', 'digits', 1, 8);

-- یک استثنای صریح آینده (مثلاً اگر یک مدل محصول به‌اشتباه با Rule دیگری تصادم کرد)
INSERT INTO technical_format_rules (rule_type, match_value, sort_order)
VALUES ('exception', 'H100', 40);
```

موتور اولویت (Tier) خودش تصادم‌های احتمالی با Ruleهای موجود را حل
می‌کند — دقیقاً همان‌طور که همین امروز واحد `Pa` را از دست Rule عمومی
`P` محافظت کرد.

---

## تصمیم‌های آگاهانه و محدودیت‌های مستند (نه باگ فراموش‌شده)

- **H2O / فرمول‌های شیمیایی**: مثل V2، عمداً به‌صورت پیش‌فرض seed نشده
  (ریسک تصادم با کد مدل مثل «H100» که نمی‌توان صرفاً با Regex از فرمول
  شیمیایی تشخیص داد). با یک Rule `auto_script` برای پیشوند `H` (حالت
  digits) توسط مدیر قابل‌فعال‌سازی است، دقیقاً مثل نمونه بالا برای `cm`.
- **واحدهای تک‌حرفی `W`، `V`، `A`**: عمداً seed نشده‌اند چون احتمال تصادم
  با کدهای مدل محصول (مثل «24V» به‌عنوان بخشی از یک کد، نه ولتاژ) بیشتر
  از واحدهای چندحرفی است. آماده‌اند، فقط نیاز به یک تصمیم آگاهانه مدیر
  دارند (نمونه INSERT بالا).
- **«Pa» بدون عدد قبلش**: اگر واژه «Pa» به‌تنهایی، بدون هیچ عددی قبلش،
  در یک جمله ظاهر شود (مثلاً «... بر حسب Pa اندازه‌گیری می‌شود»)، چون
  Rule نوع `unit` فقط با الگوی عدد+واحد فعال می‌شود، این حالت هنوز به
  Rule عمومی subscript پیشوند `P` می‌افتد و به `Pₐ` تبدیل می‌شود. در
  عمل، واحد همیشه بعد از یک عدد می‌آید (این‌طور استفاده می‌شود)، پس این
  یک محدودیت نظری مستندشده است، نه یک باگ عملی. اگر لازم شد، یک Rule
  `exception` برای «Pa» این را هم می‌بندد (به قیمت این‌که دیگر هرگز
  «Pa» به‌عنوان subscript تفسیر نمی‌شود، حتی اگر روزی واقعاً منظور
  P-زیرنویس-a باشد).
- **پنل مدیریت برای مدیریت این جدول (bulk UI)**: طبق تصمیم صریح شما در
  Session قبلی، همچنان فقط SQL/Migration است؛ در این Task هم دوباره
  ساخته نشده (خارج از Scope این دستور که فقط معماری Formatter را خواسته
  بود، نه UI).
- **Mojibake خروجی CMD**: طبق دستور صریح شما، این‌جا بررسی/تغییری روی
  Encoding فایل‌ها انجام نشد؛ صرفاً یک مشکل نمایش ترمینال است، نه مشکل
  داده واقعی (که با تست‌های بالا — که خروجی فارسی صحیح تولید کردند —
  تأیید می‌شود).
