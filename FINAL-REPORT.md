# گزارش نهایی — تسک ۶ بخشی (پنل کاربران، اعلان سفارش، فرمت واحدهای فنی، روش‌های ارسال، تومان/ریال)

منبع کد: **فقط** `tasisatapadana-site-main.zip` پیوست‌شده (Source of Truth طبق دستور). هیچ نسخه قدیمی‌تری مبنا قرار نگرفت.

---

## ۱) فایل‌های تغییرکرده/جدید

با `diff -rq` نسبت به ZIP اصلی تأیید شد که **فقط** همین فایل‌ها تغییر کرده‌اند (هیچ فایل دیگری، از جمله دسته‌بندی/محصولات/پشتیبانی/SMS، دست نخورده):

| فایل | نوع تغییر |
|---|---|
| `src/index.js` | ویرایش — همه منطق بخش‌های ۱ تا ۵ |
| `public/admin/admin-common.js` | ویرایش — دو آیتم منو (کاربران، روش‌های ارسال) |
| `public/admin/orders/orders.js` | ویرایش — نمایش روش/هزینه ارسال و پس‌کرایه در جزئیات سفارش |
| `public/store/checkout.html` | ویرایش — انتخاب روش ارسال، بروزرسانی زنده مبلغ کل |
| `public/store/order-success.html` | ویرایش — نمایش روش ارسال انتخاب‌شده |
| `public/admin/users/index.html`, `users.js` | **جدید** — صفحه مدیریت کاربران |
| `public/admin/shipping/index.html`, `shipping.js` | **جدید** — صفحه مدیریت روش‌های ارسال |
| `database/user-management.sql` | **جدید** |
| `database/order-notifications.sql` | **جدید** |
| `database/technical-format-rules.sql` | **جدید** |
| `database/shipping-methods.sql` | **جدید** |
| `database/payment-ledger.sql` | **جدید** |

دسته‌بندی/زیردسته‌بندی، سبد خرید، جست‌وجو، صفحه محصول (SSR)، KV، مسیرهای `/store/product/{slug}` و `product.html?slug=`، `<base href="/store/">`، Encoding فارسی — همگی دست‌نخورده.

## ۲) Migrationهای لازم (باید قبل از Deploy روی D1 واقعی اجرا شوند)

به همین ترتیب اجرا کنید:

```bash
npx wrangler d1 execute tasisatapadana-db --remote --file=./database/user-management.sql
npx wrangler d1 execute tasisatapadana-db --remote --file=./database/order-notifications.sql
npx wrangler d1 execute tasisatapadana-db --remote --file=./database/technical-format-rules.sql
npx wrangler d1 execute tasisatapadana-db --remote --file=./database/shipping-methods.sql
npx wrangler d1 execute tasisatapadana-db --remote --file=./database/payment-ledger.sql
```

هر ۵ فایل روی یک نسخه محلی از کل زنجیره Migration قبلی شما (با SQLite واقعی) اجرا و تأیید شدند که بدون خطا اعمال می‌شوند.

⚠️ **نکته حیاتی:** `order-notifications.sql` هنگام اجرا، برای سفارش‌های *فعلاً pending* شما (روی D1 واقعی) یک اعلان خوانده‌نشده می‌سازد؛ سفارش‌های لغوشده/تکمیل‌شده هیچ‌وقت اعلان نمی‌گیرند. این یک‌بار اجرا کافی است.

## ۳) خلاصه تغییرات هر بخش

**۱ — مدیریت کاربران:** endpoint موجود `admin/customers` گسترش یافت (`is_active`)؛ `PUT /api/store/admin/customers/:id/status` برای فعال/غیرفعال. غیرفعال‌کردن، نشست‌های جاری را هم پاک می‌کند. ورود (رمز عبور و OTP) برای حساب غیرفعال مسدود می‌شود. صفحه جدید `/admin/users/`.

**۲ — اعلان سفارش:** جدول `order_notifications` (UNIQUE روی order_id). سفارش جدید ⇒ یک اعلان. مشاهده جزئیات یا تغییر وضعیت ⇒ خوانده‌شده. Badge «جدید» = اعلان خوانده‌نشده **و** وضعیت هنوز pending — یعنی سفارش لغوشده هرگز «جدید» نیست، صرف‌نظر از تاریخچه‌اش.

**۳ — واحدها/نمادهای فنی:** جدول `technical_format_rules` (rule_type: token یا unit). پیش‌فرض: `Qn→Qₙ` و عدد+`C`→عدد+`°C`. در توضیحات، برند، مدل، مشخصات فنی (label/value) در فهرست عمومی/ادمین، جزئیات محصول، و SSR اعمال می‌شود. متن بدون الگو دست‌نخورده می‌ماند (بدون حدس).

**۴ — روش‌های ارسال:** جدول `shipping_methods` با ۴ نمونه اولیه (پست، تیپاکس، پیک اصفهان با scope شهر، پس‌کرایه). CRUD کامل در `/admin/shipping/`. Checkout حالا `shipping_method_id` می‌گیرد، سمت سرور اعتبارسنجی می‌کند (فعال بودن + مجاز بودن مقصد)، و یک هزینه مشترک برای کل سفارش محاسبه می‌کند (نه هر کالا جدا). روش غیرفعال/خارج از محدوده هرگز در پاسخ عمومی ظاهر نمی‌شود.

**۵ — تومان/ریال:** `tomanToRial`/`rialToToman` مرکزی (×۱۰، فقط یک‌بار در `initiatePaymentRequest` استفاده می‌شود). `payable_amount` سفارش = مبلغ کالاها + هزینه ارسال (فقط اگر پیش‌پرداخت باشد؛ پس‌کرایه از این مبلغ کسر است). جدول `payment_transactions` هر تلاش پرداخت را با هر دو واحد ثبت می‌کند.

⚠️ **در نسخه فعلی پروژه هیچ درگاه پرداخت واقعی متصل نیست** (سفارش‌ها همچنان دستی/حضوری تسویه می‌شوند). `initiatePaymentRequest`/`verifyPaymentTransaction` زیرساخت آماده‌اند: وقتی `env.PAYMENT_GATEWAY_API_KEY` تنظیم شود، دقیقاً همین مسیر (و همین تبدیل) باید برای درگاه واقعی هم استفاده شود.

## ۴) تست‌های انجام‌شده و نتیجه

چون این Sandbox به Cloudflare/D1 واقعی دسترسی ندارد، یک شبیه‌ساز D1 روی SQLite واقعی (`node:sqlite`) ساختیم و `src/index.js` واقعی را با Request واقعی صدا زدیم (نه شبیه‌سازی جدا).

- **زنجیره Migration:** همه ۱۳ فایل (۸ فایل قبلی + ۵ فایل جدید) روی یک schema پایه ساختگی به‌ترتیب صحیح اجرا و تأیید شد.
- **۴۷ تست خودکار اجراشده — همه موفق**، شامل:
  - فرمت‌دهی: Qn→Qₙ، عدد+C→عدد+°C، در description/brand/model/specs، هم در فهرست هم در جزئیات؛ متن بدون الگو دست‌نخورده.
  - کاربران: ثبت‌نام، فهرست با is_active، ورود موفق، غیرفعال‌سازی، مسدودشدن ورود، فعال‌سازی مجدد، ورود دوباره موفق.
  - ارسال: ۴ روش seed شده؛ پیک اصفهان فقط برای اصفهان دیده می‌شود نه تهران؛ غیرفعال‌کردن یک روش، آن را فوراً از پاسخ عمومی حذف می‌کند.
  - Checkout: رد سفارش با روش ارسال خارج از محدوده؛ محاسبه صحیح مجموع پیش‌پرداخت؛ محاسبه صحیح مجموع پس‌کرایه (هزینه ارسال از مبلغ قابل‌پرداخت آنلاین کسر می‌شود ولی در «مبلغ کل» فاکتور هست).
  - تومان/ریال: تبدیل دقیقاً ×۱۰ برای مبلغ کوچک (۱ تومان) و بزرگ (۳۷۰,۳۷۰,۳۶۷ تومان)؛ مبلغ پس‌کرایه real-گیت‌وی کمتر از کل سفارش.
  - اعلان: سفارش جدید = یک اعلان؛ سفارش آزمایشی قدیمیِ از قبل لغوشده هرگز شمرده نمی‌شود؛ مشاهده جزئیات و تغییر وضعیت هر دو باعث خوانده‌شدن می‌شوند و شمارنده کم می‌شود؛ تلاش برای درج اعلان تکراری توسط UNIQUE constraint رد می‌شود.
- **بررسی syntax:** `node --check` روی `src/index.js` و همه فایل‌های JS جدید/ویرایش‌شده، و روی اسکریپت‌های درون‌خطی `checkout.html`/`order-success.html` (استخراج‌شده) — بدون خطا.
- **بررسی عدم دست‌کاری:** `diff -rq` کامل نسبت به ZIP اصلی — فقط ۱۲ فایل بالا تغییر کرده‌اند؛ هیچ فایل دیگری لمس نشده.

فرانت‌اند جدید (`/admin/users/`, `/admin/shipping/`, بخش انتخاب روش ارسال در Checkout) در یک مرورگر واقعی تست بصری نشده (این Sandbox مرورگر ندارد)؛ فقط از نظر syntax و منطق API که به آن متصل می‌شود تأیید شده است.

## ۵) مواردی که نیاز به اقدام شما دارند

1. اجرای ۵ Migration بالا روی D1 واقعی (پیش از Deploy).
2. بعد از Deploy، حتماً یک‌بار در مرورگر واقعی چک کنید: صفحه `/admin/users/`، صفحه `/admin/shipping/`، و مسیر کامل Checkout (انتخاب شهر → دیدن روش‌های ارسال → دیدن بروزرسانی مبلغ کل → ثبت سفارش) — چون این بخش‌ها فقط منطقی/syntax تست شده‌اند، نه در مرورگر واقعی.
3. اگر می‌خواهید درگاه پرداخت واقعی وصل شود، به تابع‌های `initiatePaymentRequest`/`verifyPaymentTransaction` در `src/index.js` مراجعه کنید — نقطه اتصال دقیقاً مشخص شده.
4. قوانین فرمت‌دهی واحدهای فنی (جدول `technical_format_rules`) فقط دو نمونه پیش‌فرض دارند؛ هر واحد/نماد دیگری که لازم دارید را می‌توانید مستقیماً با یک ردیف INSERT به همین جدول اضافه کنید (بدون Deploy).

## ۶) وضعیت Deploy

Deploy واقعی از این Sandbox ممکن نیست (بدون دسترسی اینترنت/Cloudflare واقعی، طبق توضیح داده‌شده در جلسات قبلی). فایل نهایی برای Deploy توسط شما آماده است.
