# گزارش رفع باگ — تصویر محصول در سبد خرید هنگام افزودن از صفحه فروشگاه

## ۱. علت دقیق باگ

در `public/store/store.js`، تابع `renderProducts()` برای نمایش تصویر روی کارت محصول، مسیر تصویر را کامل «Resolve» می‌کرد (اگر خالی بود از `images[0]` استفاده می‌کرد و اگر نام فایل خام بود، پیشوند `/assets/products/` را اضافه می‌کرد) — دقیقاً همان منطقی که در `product.html` هم به‌صورت تابع `getProductImageUrl()` وجود دارد.

اما تابع `addToCart()` در همان فایل، به‌جای استفاده از همین مقدار Resolve‌شده، مستقیماً فیلد خام `product.image` را در آبجکت سبد خرید ذخیره می‌کرد:

```js
image: product.image || "",
```

یعنی اگر:
- محصول فقط از طریق آرایه `images[]` (جدول `product_images`) تصویر داشت (نه فیلد تکی `image`) → مقدار ذخیره‌شده در سبد **خالی** می‌ماند.
- فیلد `image` یک نام فایل خام بدون پیشوند بود (مثلاً `boutan.jpg`) → همان رشته خام بدون تبدیل به `/assets/products/boutan.jpg` در سبد ذخیره می‌شد.

سپس در `public/store/cart.html`، تابع `renderCart()` مقدار `item.image` را بدون هیچ اصلاحی مستقیماً در `<img src="...">` قرار می‌داد:

```js
src="${escapeHtml(item.image || "../assets/products/placeholder.svg")}"
```

بنابراین یک مسیر نسبی/خام مثل `boutan.jpg` نسبت به آدرس صفحه `cart.html` (یعنی `/store/`) Resolve می‌شد و به یک URL نادرست/۴۰۴ منجر می‌شد — دقیقاً همان چیزی که در فروشگاه دیده می‌شد.

در طرف مقابل، `product.html` (صفحه جزئیات محصول) از ابتدا این مشکل را نداشت چون `addProductToCart()` در آن‌جا از همان تابع `getProductImageUrl()` استفاده می‌کند:

```js
image: productImages[0] || getProductImageUrl(currentProduct.image),
```

پس علت اصلی: **یک منطق Resolve مسیر تصویر، در دو مسیر مختلف کد به‌طور ناهم‌سان اعمال می‌شد** — در `product.html` قبل از ذخیره در سبد اعمال می‌شد، ولی در `store.js` فقط برای نمایش کارت اعمال می‌شد و هرگز قبل از ذخیره در سبد اجرا نمی‌شد.

---

## ۲. فایل‌های تغییرکرده

فقط همین دو فایل (بدون هیچ فایل دیگری، بدون Migration، بدون تغییر در D1):

- `public/store/store.js`
- `public/store/cart.html`

هیچ فایل جدیدی ایجاد نشد.

---

## ۳. اصلاح دقیق انجام‌شده

### `store.js`
- منطق Resolve مسیر تصویر (که قبلاً درون `renderProducts` تکرار شده بود) در یک تابع مستقل و قابل‌استفاده مجدد استخراج شد: `getProductImageUrl(image)` — دقیقاً همان منطق فعلی `product.html` (شامل موارد خاص `assets/` و `products/` که نسخه قبلی store.js نداشت، تا رفتار هر دو صفحه کاملاً یکسان شود).
- یک تابع کمکی کوچک اضافه شد: `getPrimaryProductImage(product)` که همان اولویت قبلی (`image` تکی، سپس اولین آیتم `images[]`) را حفظ می‌کند و در نهایت از `getProductImageUrl` عبور می‌دهد.
- `renderProducts()` اکنون از همین `getPrimaryProductImage()` برای تصویر کارت استفاده می‌کند (بدون تغییر در رفتار قابل مشاهده کارت).
- `addToCart()` اصلاح شد تا هنگام افزودن آیتم جدید، به‌جای `product.image` خام، از `getPrimaryProductImage(product)` استفاده کند. یعنی همان تصویر واقعی و صحیحی که روی کارت دیده می‌شود، اکنون در سبد هم ذخیره می‌شود.

### `cart.html`
- همان تابع `getProductImageUrl(image)` (کاملاً یکسان با نسخه `product.html`/`store.js`) به‌عنوان یک لایه دفاعی اضافه شد.
- `renderCart()` اصلاح شد تا به‌جای استفاده مستقیم از `item.image`، آن را از `getProductImageUrl(item.image)` عبور دهد. این کار دو اثر دارد:
  1. آیتم‌های تازه (که از این پس همیشه یک مسیر صحیح ذخیره می‌کنند) بدون تغییر نمایش داده می‌شوند (تابع روی مسیر مطلق/کامل تأثیری ندارد، idempotent است).
  2. آیتم‌های **قدیمی** که قبل از این رفع باگ، با مقدار خالی یا مسیر خام ذخیره شده بودند، در لحظه نمایش (نه با تغییر `localStorage`) به مسیر صحیح یا placeholder امن اصلاح می‌شوند — بدون این‌که سبد خرید فعلی کاربر خراب یا پاک شود.

هیچ تغییری در Checkout، سفارش‌ها، موجودی، دسته‌بندی، روابط محصولات یا Feature Flagها اعمال نشد (بررسی شد که هیچ فایل دیگری از مقدار `item.image` استفاده نمی‌کند).

---

## ۴. تست‌های انجام‌شده و نتیجه

تمام تست‌ها با اجرای واقعی همین دو فایل (نه شبیه‌سازی) در Node.js انجام شد — **۱۷ از ۱۷ مورد موفق**:

| # | تست | نتیجه |
|---|---|---|
| 1 | مسیر A (`/store/` → افزودن به سبد): محصول وارد سبد می‌شود | ✅ |
| 2 | مسیر A: تصویر خام (`boutan.jpg`) به `/assets/products/boutan.jpg` تبدیل و در سبد ذخیره می‌شود | ✅ |
| 3 | مسیر A: `cart.html` هم همان مسیر صحیح را برای `<img>` تولید می‌کند | ✅ |
| 4 | مسیر B (`getProductImageUrl` در `product.html`): با URL کامل بدون تغییر کار می‌کند (بدون Regression) | ✅ |
| 5 | مسیر B: با نام فایل خام درست عمل می‌کند (بدون Regression) | ✅ |
| 6 | مسیر B: بدون تصویر → placeholder (بدون Regression) | ✅ |
| 7 | محصول بدون تصویر (نه `image`، نه `images[]`): بدون خطا، placeholder ذخیره می‌شود | ✅ |
| 8 | محصول با آرایه `images[]` و بدون `image` تکی: از `images[0]` استفاده و Resolve می‌شود | ✅ |
| 9 | چند محصول مختلف هم‌زمان در سبد | ✅ |
| 10 | افزایش تعداد یک محصول تکراری (به‌جای ایجاد آیتم جدید) | ✅ |
| 11 | محصولی که از قبل مسیر مطلق تصویر داشت، دست‌نخورده باقی می‌ماند | ✅ |
| 12 | کاهش تعداد از داخل صفحه سبد (`changeQuantity`) صحیح اعمال می‌شود | ✅ |
| 13 | Refresh صفحه سبد (رندر مجدد از `localStorage`) بدون خطا | ✅ |
| 14 | سبد قدیمی با آیتم بدون فیلد `image` اصلاً: بدون خطا، placeholder نمایش داده می‌شود | ✅ |
| 15 | سبد قدیمی با مسیر خام قدیمی (`old-raw.jpg`): در لحظه نمایش به مسیر صحیح تصحیح می‌شود | ✅ |
| 16 | رندر یک سبد قدیمی مخلوط (بدون-image + مسیر-خام) بدون خطا | ✅ |
| 17 | داده خام `localStorage` کاربر دست‌نخورده می‌ماند (فقط نمایش اصلاح می‌شود، نه ذخیره‌سازی) | ✅ |

همچنین `node --check` روی `store.js` و اسکریپت داخلی `cart.html` بدون خطا اجرا شد.

---

## نکته

فایل جدیدی ایجاد نشد؛ فقط `store.js` و `cart.html` اصلاح شدند.
