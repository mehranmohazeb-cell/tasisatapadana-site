# گزارش نهایی — ویرایش سوم (معماری عمومی محتوای فنی + Shipping Class/Table Rate)

منبع: **فقط** آخرین ZIP تحویلی همین گفت‌وگو (`tasisatapadana-site-edit2.zip`). `diff -rq` نسبت به آن تأیید کرد تغییرات **فقط** به فایل‌های زیر محدود است؛ هیچ بازطراحی/تغییر نامرتبطی (رنگ، Layout، Header/Footer، URL) رخ نداده.

## فایل‌های تغییرکرده

`src/index.js`، `public/admin/admin-common.js`، `public/admin/products/index.html`، `public/admin/products/products.js`، `public/admin/shipping/index.html`، `public/admin/shipping/shipping.js`، `public/store/cart.html`، `public/store/store.css`

## فایل‌های جدید

- `database/technical-format-rules-v2.sql`
- `database/shipping-classes-and-rates.sql`
- `public/admin/shipping/classes/index.html`, `classes.js`
- `public/admin/shipping/table-rates/index.html`, `table-rates.js`

## Migrationها

### `technical-format-rules-v2.sql`
**دلیل:** rule_type قبلی با CHECK محدود به `('token','unit')` بود؛ برای افزودن نوع عمومی جدید `auto_script`، طبق قاعده استاندارد SQLite جدول بازسازی شد (ساخت جدول جدید → کپی کامل داده → حذف قدیمی → تغییرنام) — **هیچ داده‌ای از دست نرفت**، فقط قانون قدیمی `Qn` (token) با قانون عمومی جایگزین شد.
**تغییرات:** ستون جدید `script_type`؛ مقدار مجاز جدید `auto_script` برای `rule_type`؛ دو قانون seed جدید (`Q`، `P` با `script_type='subscript'`).
**اجرای Remote لازم است:** بله.

### `shipping-classes-and-rates.sql`
**دلیل:** پیاده‌سازی Shipping Class + Table Rate Shipping (بخش دوم دستور) نیازمند جدول‌های جدید بود.
**تغییرات:** جدول‌های `shipping_classes` و `shipping_table_rates` (جدید)؛ ستون‌های Nullable جدید روی `products`: `shipping_class_id`, `weight_grams`, `length_cm`, `width_cm`, `height_cm`.
**اجرای Remote لازم است:** بله. کاملاً افزایشی — محصولات فعلی بدون این اطلاعات دقیقاً مثل قبل کار می‌کنند.

⚠️ Migrationهای قبلی (`user-management`, `order-notifications`, `technical-format-rules` نسخه ۱، `shipping-methods`, `payment-ledger`, `product-shipping-rates`) **دوباره اجرا نشدند**.

```bash
npx wrangler d1 execute tasisatapadana-db --remote --file=./database/technical-format-rules-v2.sql
npx wrangler d1 execute tasisatapadana-db --remote --file=./database/shipping-classes-and-rates.sql
```

## بخش اول — معماری عمومی محتوای فنی

**مشکل قبلی:** هر نماد (Qn) یک Rule جدا و حدسی بود. **راه‌حل:** نوع قانون کاملاً عمومی `auto_script` — یک پیشوند (مثلاً `Q`) + `script_type` (subscript/superscript). با همین **دو قانون** (`Q`، `P`)، تمام ترکیب‌های خواسته‌شده به‌صورت خودکار پوشش داده می‌شوند: `Qn→Qₙ`, `Qm→Qₘ`, `Qmax→Qₘₐₓ`, `Qmin→Qₘᵢₙ`, `Pn→Pₙ`, `Pm→Pₘ` — **بدون افزودن هیچ Rule دیگری**. برای هر پیشوند/فرمول آینده (مثلاً یک روز `V` یا `R`)، مدیر فقط یک ردیف در دیتابیس اضافه می‌کند؛ نیازی به تغییر کد نیست.

**H2O:** همان مکانیزم به‌صورت Opt-in برای هر پیشوند حرفی کار می‌کند (تست شد با افزودن یک قانون `auto_script` با پیشوند `H`) — چون این ریسک برخورد با کدهای مدل (مثل «H100») دارد، به‌صورت پیش‌فرض Seed نشده؛ اضافه‌کردنش تصمیم مدیر است.

**ایمنی در برابر تبدیل حدسی:** فقط نویسه‌هایی که معادل رسمی یونیکد زیرنویس/بالانویس دارند تبدیل می‌شوند (نگاشت صریح، نه حدس)؛ حروف بزرگ هرگز تبدیل نمی‌شوند (پس «O» در H2O دست‌نخورده می‌ماند)؛ سقف ۳ نویسه بعد از پیشوند باعث می‌شود کلمات معمولی طولانی‌تر (مثل Quality، Quick) هرگز اشتباهی نماد فنی تلقی نشوند — تست شد.

**Rich Text واقعی (بخش ۷):** Toolbar موجود فرم محصول (که قبلاً فقط Bold/لیست بولت/تیتر/لینک داشت) گسترش یافت: H2/H3/H4، پاراگراف، Bold، *Italic (جدید)*، لیست بولت، **لیست شماره‌دار (جدید)**، لینک، و **دکمه درج جدول فنی (جدید)**.

**جدول‌های فنی (بخش ۸):** دکمه «جدول» یک `<table>` واقعی (thead/tbody/th/td) در محل نشانگر درج می‌کند. `sanitizeDescriptionHtml` موجود از قبل یک Sanitizer مبتنی بر Blocklist بود (فقط script/رویدادهای خطرناک/`javascript:` را حذف می‌کند)، نه Allowlist محدود — پس تگ‌های جدول/تیتر/لیست از قبل بدون نیاز به تغییر، سالم عبور می‌کنند (با تست واقعی تأیید شد). استایل حداقلی جدول به `store.css` اضافه شد (فقط border/padding، هماهنگ با طراحی فعلی).

**جداسازی داده خام از نمایش (بخش ۶):** بدون تغییر — قوانین فقط در لحظه پاسخ اعمال می‌شوند؛ HTML خام محصول در D1 دست‌نخورده و قابل جست‌وجو می‌ماند.

**سازگاری با Word (بخش ۹):** چون محتوا همان HTML استاندارد (h1-h6/p/b/i/ul/ol/li/a/table) است، معماری از قبل برای «Word→HTML→ویرایش در همین Editor» آماده است؛ قابلیت Import واقعی طبق دستور در این مرحله ساخته نشد (به‌درستی خارج از دامنه).

**اعمال روی محتوای غیر از محصول (بخش ۵):** موتور فرمت‌دهی (`formatTechnicalText`) یک تابع کاملاً عمومی است که هر رشته متنی را می‌گیرد — از قبل به «محصول» وابسته نیست. یک ویژگی «مقالات آموزشی» به‌عنوان نوع محتوای جدید در این مرحله ساخته نشد (چیزی در دستور نبود که صراحتاً بخواهد الان ساخته شود؛ فقط خواسته بود موتور عمومی و قابل‌استفاده باشد که هست).

## بخش دوم — Shipping Class + Table Rate Shipping

**بدون سیستم موازی** — دقیقاً روی `shipping_methods` و `product_shipping_rates` موجود سوار شد.

**اولویت محاسبه هزینه هر محصول (مستند در کد و اینجا، بخش ۱۴):**
1. **`product_shipping_rates`** — استثنای دستی همان محصول برای همان روش (`custom_cost`)، اگر تعریف شده باشد.
2. **`shipping_table_rates`** — بالاترین Priority که با Shipping Class محصول + شرایط کل سبد (وزن کل، تعداد کل، مبلغ کل، مقصد) سازگار باشد.
3. **`shipping_methods.cost`** — هزینه پیش‌فرض سراسری (Fallback نهایی؛ رفتار محصولات بدون Shipping Class دقیقاً مثل قبل از این ویرایش).

هر شرط Table Rate اختیاری است (NULL = بدون محدودیت روی آن شرط)؛ افزودن شرط جدید در آینده فقط با یک ستون Nullable جدید ممکن است، بدون بازنویسی منطق تطبیق.

**سبد چندمحصولی:** طبق تصمیم معماری قبلی (حفظ‌شده)، هزینه نهایی هر روش = بیشترین هزینه (طبق کاسکید بالا) در بین محصولات سبد؛ اگر محصولی روش را غیرمجاز کرده باشد، آن روش برای کل سبد حذف می‌شود.

**وزن/ابعاد محصول:** ستون‌های `weight_grams` (گرم)، `length_cm`/`width_cm`/`height_cm` (سانتی‌متر) — واحد ثابت و ساده برای محاسبات آینده.

**مدیریت از پنل (بخش ۱۶):** سه بخش کاملاً مجزا با زیرمنوی مشترک «روش‌های ارسال»:
- `/admin/shipping/` — Shipping Methods (موجود، دست‌نخورده)
- `/admin/shipping/classes/` — Shipping Classes (جدید: افزودن/ویرایش/فعال‌سازی/حذف امن)
- `/admin/shipping/table-rates/` — Table Rates (جدید: فرم کامل شرایط + اولویت + خروجی CSV)
- فرم ویرایش محصول (`/admin/products/`) — انتخاب Shipping Class + وزن/ابعاد اضافه شد (بدون تغییر در محل موجود «قواعد ارسال این محصول» که از ویرایش قبلی مانده)

**CSV (بخش ۱۷):** فقط **Export** برای Table Rates پیاده‌سازی شد (`GET /api/store/admin/shipping-table-rates/export.csv`، از پنل با دکمه دانلود واقعی). **Import پیاده‌سازی نشده** — طبق اجازه صریح دستور، چون پیاده‌سازی کامل و امن Import (اعتبارسنجی ردیف‌به‌ردیف، خطاهای جزئی، جلوگیری از داده ناقص) حجم قابل‌توجهی کار مجزا می‌طلبید. ساختار Database/Backend (جدول‌ها + Endpointهای CRUD موجود) کاملاً آماده است که یک Endpoint Import در مرحله بعد روی همین جدول‌ها اضافه شود.

## سازگاری Cart/Checkout/Estimate (بخش ۱۸)

هر سه (صفحه محصول، سبد، Checkout) دقیقاً از یک تابع مشترک (`resolveShippingOptionsForCart`) با همین کاسکید جدید استفاده می‌کنند — تفاوت تنها در دقت داده ورودی: صفحه محصول تعداد=۱ فرض می‌کند (چون هنوز تعداد انتخاب نشده)؛ سبد تعداد واقعی هر قلم را می‌فرستد؛ Checkout علاوه‌بر تعداد، قیمت واقعی هر قلم را هم می‌فرستد (برای شرط «مبلغ سبد»). این تفاوت مستند و منطقی است، نه اختلاف محاسباتی واقعی — Checkout همیشه مرجع نهایی و کاملاً دقیق است.

## تست‌های انجام‌شده

با همان روش قبلی (D1 روی `node:sqlite`، فراخوانی واقعی `src/index.js`): **۸۳ تست خودکار — همه موفق** (۵۹ تست قبلی + ۲۴ تست جدید)، شامل:
- **محتوای فنی:** Qn→Qₙ، Qmax→Qₘₐₓ، Pm→Pₘ (سه‌تا با فقط دو Rule)، H2O→H₂O (Opt-in)، °C (رگرسیون)، عدم خراب‌شدن کلمات معمولی (Quality/Quick)، عدم خراب‌شدن HTML/جدول/API/SSR.
- **Rich Text:** Heading، پاراگراف، Bold/Italic (از طریق Sanitizer)، لیست شماره‌دار، لینک، Table، و **فرمت‌دهی فنی داخل سلول جدول** (ترکیب دو ویژگی با هم تست شد).
- **Shipping:** محصول بدون Class (Fallback به پیش‌فرض روش)، محصول با Class (Table Rate)، اولویت ۱>۲>۳ (استثنای محصول > Table Rate > پیش‌فرض) با تست مستقیم، چند Shipping Class، روش غیرمجاز/مجاز، وزن (آستانه Table Rate)، محاسبه Cart/Checkout، حذف Class درحال‌استفاده مسدود می‌شود، سازگاری با Shipping Methods/product_shipping_rates فعلی.
- **Regression کامل:** کاربران، اعلان سفارش، فاکتور/تصویر، تومان-ریال، COD، Checkout — همه ۵۹ تست قبلی دوباره اجرا و موفق.

## نیاز به تست دستی واقعی (این Sandbox مرورگر ندارد)

- ظاهر و رفتار واقعی Toolbar جدید Rich Text (Italic، لیست شماره‌دار، دکمه جدول) در مرورگر — فقط از نظر Syntax/تولید HTML صحیح تست شده.
- صفحات جدید `/admin/shipping/classes/` و `/admin/shipping/table-rates/` — فرم‌ها و دکمه دانلود CSV.
- نمایش بصری جدول‌های فنی داخل توضیحات محصول در صفحه واقعی محصول.
- فیلدهای جدید وزن/ابعاد/Shipping Class در فرم ویرایش محصول.

## موارد باقی‌مانده برای مرحله بعد (صریحاً اعلام)

- **Import CSV** برای Table Rates (فقط Export ساخته شد؛ دلیل و آمادگی Backend در بالا توضیح داده شد).
- ویژگی «مقالات آموزشی» به‌عنوان نوع محتوای مستقل (موتور فرمت‌دهی از قبل برایش آماده است، اما خودِ صفحه/جدول مقالات ساخته نشده — چیزی در دستور نبود که بخواهد الان ساخته شود).
- Import واقعی از Word (معماری آماده است؛ ابزار Import طبق دستور در این مرحله ساخته نشد).
- مدیریت گروهی (Bulk) قوانین `technical_format_rules` از پنل ادمین — فعلاً فقط از طریق Migration/SQL قابل مدیریت است (این بخش در دستور صراحتاً خواسته نشده بود).

هیچ‌کدام از موارد بالا وانمود به تکمیل نشده‌اند؛ همگی به‌صراحت به‌عنوان «باقی‌مانده» ثبت شدند.
