# گزارش Final Hardening — Technical Formatting V3

معماری V3 (Tier-based conflict resolution) بدون تغییر مانده؛ این دور فقط
دو ریسک باقی‌مانده را بست و قابلیت‌های خواسته‌شده را تست کرد.

## ۱. دقیقاً چه چیزی تغییر کرد؟

فقط یک تغییر کد، در یک فایل: در `src/technical-format.js`، شاخهٔ
`rule_type === "unit"` یک حالت دوم پیدا کرد. قبلاً این شاخه فقط یک الگو
داشت («عدد + واحد»). حالا یک الگوی دوم هم دارد: «فقط واحد، بدون نیاز به
عدد قبلش» — که بازهٔ همان واحد را در همان Tier (۲) رزرو می‌کند، اما
جایگزینی آن **بدون تغییر** (خودِ `match_value`) است، نه `display_value`.

نتیجه: وقتی عدد وجود دارد، تطابق «عدد+واحد» بلندتر است و طبق قانون
تصادم موجود (در Tier مساوی، تطابق بلندتر برنده است) خودش برنده می‌شود —
پس رفتار قبلی («۶۰ C» → «۶۰ °C») دقیقاً حفظ شده. وقتی عدد وجود ندارد،
همین حالت دوم به‌تنهایی وارد عمل می‌شود و بازه را با Tier ۲ (واحد) رزرو
می‌کند — که همیشه از Tier ۱ (`auto_script`) بالاتر است — بدون این‌که
هیچ تغییری در متن اعمال کند.

هیچ فایل دیگری در این دور تغییر نکرد (نه `src/index.js`، نه Migration
جدید) — با `diff` مستقیم علیه تحویل قبلی V3 تأیید شد.

## ۲. چرا این تغییر لازم بود؟

چون راه‌حل قبلی (الگوی «عدد+واحد») فقط وقتی واحد بلافاصله بعد از یک عدد
می‌آمد، آن بازه را رزرو می‌کرد. اگر واژهٔ «Pa» بدون هیچ عددی قبلش در متن
ظاهر می‌شد، هیچ Rule‌ای آن بازه را رزرو نکرده بود، پس Rule عمومی‌تر و
Tier‌پایین‌تر (`auto_script` پیشوند P) آزاد بود آن را بگیرد و به «Pₐ»
تبدیل کند. راه‌حل، دقیقاً طبق خواستهٔ شما، یک Exception موردی برای رشتهٔ
«Pa» نبود؛ بلکه تعمیم همان مکانیزم رزرو-بر-اساس-Tier بود به «خودِ نام
واحد»، مستقل از این‌که عدد قبلش هست یا نه — پس این اصل برای **هر** واحدی
که در جدول تعریف شود (چه امروز، چه در آینده) به‌طور خودکار برقرار است.

## ۳. تست «Pa» بدون عدد چه نتیجه‌ای داد؟

Pass. با Ruleهای واقعاً Migrate‌شده (v1+v2+v3، بدون هیچ تغییر Schema):

- `"Pa"` (تنها) → `"Pa"`
- `"فشار بر حسب Pa اندازه‌گیری می‌شود"` → دقیقاً همان، بدون تغییر
- `"واحد آن kPa است"` (بدون عدد) → دقیقاً همان، بدون تغییر

## ۴. تست «1500 Pa» چه نتیجه‌ای داد؟

Pass، و بدون تغییر نسبت به V3:
- `"1500 Pa"` → `"1500 Pa"`
- `"1.5 MPa"` → `"1.5 MPa"` (بررسی شد که رزرو مستقل واحد «Pa» داخل رشتهٔ
  «MPa» تداخلی ایجاد نمی‌کند؛ چون تطابق واحد «MPa» بلندتر است و برنده می‌شود)

## ۵. تست قابلیت آینده «W/V/A» چه نتیجه‌ای داد؟

Pass — قابلیت واقعاً در معماری موجود است، بدون هیچ تغییر کد اضافه:
با افزودن سه Rule فرضی از نوع `unit` برای V، W، A (فقط در تست، **در
Migration/Seed واقعی اضافه نشدند**، طبق خواستهٔ شما):

- `"220V"` → `"220 V"`, `"24W"` → `"24 W"`, `"10A"` → `"10 A"`
  (فاصله‌گذاری صحیح، دقیقاً مثل بقیهٔ واحدها)
- `"ولتاژ با نماد V نشان داده می‌شود"` (بدون عدد) → بدون تغییر (رزروشده)
- حتی وقتی یک `auto_script` فرضی روی پیشوند «V» هم به‌صورت آزمایشی اضافه
  شد (شبیه‌سازی بدترین حالت تصادم آینده)، Rule نوع `unit` همچنان با Tier
  بالاتر برنده ماند — نه «220V» به‌جایی اشتباه تبدیل شد، نه «V» تنها.

## ۶. تست «H2O» چه نتیجه‌ای داد؟

طبق دستور، کد بدون دلیل تغییر نکرد؛ فقط پیکربندی درخواستی دقیقاً همان‌طور
که خواسته شده بود تست شد: `prefix=H`, `script_type=subscript`,
`suffix_mode=digits`. نتیجهٔ صادقانه (نه موفق):

| ورودی | با `suffix_mode=digits` (دقیقاً پیکربندی درخواستی) |
|---|---|
| `H2O` | `H2O` — **تبدیل نمی‌شود** |
| `H100` | `H₁₀₀` — **اشتباه تبدیل می‌شود** |
| `H24` | `H₂₄` — **اشتباه تبدیل می‌شود** |
| `H2` | `H₂` — **اشتباه تبدیل می‌شود** |

دلیل فنی: با `suffix_mode=digits`، فقط رقم مجاز است و بلافاصله بعد از
دنبالهٔ رقمی باید مرز کلمه (`\b`) باشد. در «H2O»، بعد از رقم «۲» بلافاصله
حرف «O» می‌آید — که خودش هم حرف است، پس مرزی بین این دو وجود ندارد و
تطابق کلاً شکست می‌خورد (H2O دست‌نخورده می‌ماند). اما در «H100»/«H24»/«H2»،
بعد از دنبالهٔ رقمی یا پایان رشته می‌آید یا کاراکتر غیرکلمه‌ای — که خودش
مرز محسوب می‌شود، پس این‌ها (اشتباهاً) تبدیل می‌شوند.

نتیجه: **این پیکربندی خاص (`digits`) نه‌تنها H2O را حل نمی‌کند، بلکه
دقیقاً همان کدهای مدلی را که قرار بود ازش محافظت کند (H100، H24، H2) هم
اشتباه تبدیل می‌کند.** برای مقایسه، همان تست با `suffix_mode=alnum` (که
مستندسازی V2/V3 قبلی هم به آن اشاره کرده بود) هم اجرا شد:

| ورودی | با `suffix_mode=alnum` |
|---|---|
| `H2O` | `H₂O` — درست |
| `H100` | `H₁₀₀` — همان ریسک شناخته‌شدهٔ قبلی |
| `H24` | `H₂₄` — همان ریسک شناخته‌شدهٔ قبلی |
| `H2` | `H₂` — همان ریسک شناخته‌شدهٔ قبلی |

جمع‌بندی صادقانه: موتور فعلی (چه با `digits` چه با `alnum`) به‌تنهایی
نمی‌تواند «H2O» را از «H100/H24/H2» صرفاً با تنظیمات موجود (suffix_mode،
max_suffix_len) تشخیص دهد — چون تفاوت واقعی این دو گروه («فرمول شیمیایی»
در برابر «کد مدل») یک تفاوت **معنایی** است، نه یک تفاوت ساختاری قابل‌بیان
با این دو Regex-knob. طبق دستور صریح شما («کد را بی‌دلیل تغییر نده»)، این
یافته فقط گزارش شد و **هیچ کدی برای حل این تفاوت معنایی تغییر نکرد**؛
H2O طبق خواستهٔ شما در این دور هم Seed نشد.

## ۷. تعداد تست‌های Regression و نتیجه

- Regression کامل V3 (۵۶ بررسی از Session قبل، شامل Subscript/Superscript/
  واحدها/False positive/Idempotency/HTML-safety روی محصول واقعی): **۵۶ از
  ۵۶ Pass** (بدون تغییر نسبت به قبل).
- تست‌های اختصاصی همین دور (Pa بدون/با عدد، MPa، kPa بدون عدد، قابلیت
  W/V/A، و بخش Regression تکراری V3 روی این تست فایل جدید): **۳۸ از ۳۸
  Pass** (بخش H2O توضیحی/اکتشافی است، نه Pass/Fail، چون هدفش گزارش صادقانه
  بود نه تأیید یک انتظار از پیش).
- جمع کل بررسی‌شده در این دور (با احتساب تست‌های لایه ۱ که در V3 هم دوباره
  اجرا شدند): **۹۴ بررسی، ۰ شکست**.

## ۸. فایل‌های تغییرکرده

فقط `src/technical-format.js` (یک تغییر متمرکز در شاخهٔ `unit`). هیچ فایل
دیگری — نه `src/index.js`، نه هیچ فایل زیر `public/`، نه `wrangler.toml` —
تغییر نکرد. با `diff` مستقیم علیه تحویل قبلی (`tasisatapadana-site-technical-format-v3.zip`)
تأیید شد که `src/index.js` بایت‌به‌بایت یکسان مانده است.

## ۹. آیا Migration جدید لازم است؟

خیر. این اصلاح کاملاً در سطح موتور (کد) بود؛ هیچ ستون یا rule_type جدیدی
لازم نداشت — Ruleهای موجود (`Pa`، `kPa`، `MPa`، ...) دقیقاً همان ردیف‌هایی
هستند که در Migration نسخه ۳ (`technical-format-rules-v3.sql`) قبلاً روی
D1 اجرا کرده‌اید؛ چیزی تکرار یا بازطراحی نشد.

## ۱۰. تأیید Scope

با `diff -rq` روی کل درخت پروژه (نسخهٔ اصلی در برابر نسخهٔ نهایی) تأیید شد
که **به‌جز همان یک فایل بالا**، هیچ بخش دیگری از پروژه — Shipping، Payment،
Account، Invoice، CSS، UI عمومی، `public/`، `wrangler.toml` — تغییر نکرده
است.

---

نسخهٔ نهایی به‌صورت ZIP کامل پروژه پیوست است.
