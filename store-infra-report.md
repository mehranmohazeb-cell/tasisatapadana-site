# گزارش تحویل — اصلاح موجودی، کپی شماره سفارش، زیرساخت دسته‌بندی و روابط محصولات

## ۱. فایل‌های تغییرکرده / جدید

### Backend
- `src/index.js` — ویرایش شد (additive):
  - اضافه شدن `category_id` به SELECT/INSERT/UPDATE محصولات (فهرست عمومی، فهرست ادمین، تکی، ایجاد، ویرایش)
  - Helperهای جدید: `getCategoriesFlat`, `getProductRelationsGrouped`, `updateCoPurchases`, `getCoPurchasedProducts`, `getFeatureFlags`
  - Endpointهای جدید ادمین: `/api/store/admin/categories` (GET/POST/PUT/DELETE)، `/api/store/admin/product-relations` (GET/POST/DELETE)، `/api/store/admin/feature-flags` (GET/POST)، `/api/store/admin/products/search` (GET)
  - Endpoint عمومی جدید: `POST /api/store/cart/suggestions`
  - `GET /api/store/products/:slug` — دو فیلد اختیاری `related_products`/`similar_products` اضافه شد (پشت پرچم)
  - Checkout (`POST /api/store/checkout`) — یک فراخوانی غیربحرانی به `updateCoPurchases` بعد از ثبت موفق `order_items` اضافه شد

### Database
- `database/categories-and-relations.sql` (**جدید**) — تنها Migration جدید این مرحله

### Frontend فروشگاه
- `public/store/store.js` — اصلاح باگ موجودی در کارت محصول + سخت‌گیری در `addToCart`
- `public/store/order-success.html` — دکمه کپی کد پیگیری
- `public/store/store.css` — استایل دکمه کپی (`.copy-btn`)

### پنل مدیریت
- `public/admin/admin-common.js` — آیتم ناوبری «دسته‌بندی» اضافه شد
- `public/admin/admin-common.css` — استایل‌های عمومی مدیریت روابط محصول (چیپ/جست‌وجو) اضافه شد
- `public/admin/categories/index.html`, `categories.js`, `categories.css` (**جدید**) — صفحه مدیریت دسته‌ها + پرچم‌های نمایش عمومی
- `public/admin/products/index.html`, `products.js` — انتخابگر دسته + بخش «روابط محصول» (فقط هنگام ویرایش محصول موجود)

هیچ فایل دیگری (صفحه اصلی، هدر، فوتر، Checkout، سفارش‌ها، پشتیبانی، پیامک) لمس نشد.

---

## ۲. تغییرات D1 / Migration جدید

فایل: `database/categories-and-relations.sql`

| بخش | نوع تغییر |
|---|---|
| `categories` | جدول جدید (خودارجاع با `parent_id`، عمق نامحدود) |
| `products.category_id` | ستون جدید nullable (FK به `categories`) |
| `product_relations` | جدول جدید (`relation_type` متنی و باز برای توسعه) |
| `product_co_purchases` | جدول جدید (شمارنده تجمیعی خرید مشترک) |
| `site_settings` | ۴ ستون جدید boolean (پیش‌فرض ۰) برای پرچم‌های نمایش عمومی |

**بدون DROP/DELETE/TRUNCATE.** این Migration را روی D1 واقعی اجرا کنید:

```bash
npx wrangler d1 execute tasisatapadana-db --remote --file=./database/categories-and-relations.sql
```

✅ این Migration را با یک اسکریپت Python/SQLite واقعی، روی یک کپی از تمام جدول‌های فعلی (به‌همراه همه Migrationهای قبلی پروژه به ترتیب) اجرا کردم تا مطمئن شوم بدون خطا روی پایگاه‌داده واقعی هم اعمال می‌شود و محصولات موجود دست‌نخورده می‌مانند.

---

## ۳. اصلاح کنترل موجودی

**ریشه مشکل:** در `store.js`، تابع رندر کارت محصول اصلاً به `stock` نگاه نمی‌کرد (برخلاف صفحه جزئیات محصول که قبلاً درست بود).

**اصلاح:**
- کارت محصول اکنون دقیقاً همان شرط صفحه جزئیات را دارد: `Number(product.stock) > 0`. اگر ناموجود باشد، دکمه `disabled` می‌شود و متن به «موجودی تمام شده» تغییر می‌کند.
- تابع `addToCart` هم یک لایه دفاعی اضافه گرفت: حتی اگر کسی از کنسول مرورگر مستقیماً این تابع را صدا بزند، دوباره موجودی و مقدار را چک می‌کند.
- **Backend از قبل امن بود** — Endpoint واقعی ثبت سفارش (`POST /api/store/checkout`) پیش از این هم موجودی را در لحظه ثبت با یک شرط اتمیک (`UPDATE products SET stock = stock - ? WHERE id = ? AND stock >= ?`) بررسی و کم می‌کرد، و اگر شرط برقرار نبود سفارش با خطا رد می‌شد. یعنی حتی قبل از این تغییر هم دستکاری مستقیم HTTP برای خرید محصول ناموجود ممکن نبود؛ فقط نمایش Frontend در صفحه فروشگاه اشتباه بود.
- این رفتار enforcement با یک سفارش واقعی آزمایشی روی یک محصول با موجودی صفر تأیید شد (رد شد، بدون تغییر در جدول `products`/`orders`).

---

## ۴. قابلیت کپی شماره سفارش

- در `order-success.html`، کنار کد پیگیری یک دکمه «📋 کپی» اضافه شد.
- از `navigator.clipboard.writeText` استفاده می‌کند و در صورت نبود دسترسی (مثلاً بستر ناامن یا مرورگر قدیمی)، به `document.execCommand('copy')` روی یک textarea مخفی سقوط می‌کند.
- بعد از موفقیت، متن دکمه موقتاً به «✅ کپی شد» تغییر می‌کند (۱.۸ ثانیه) و دوباره غیرفعال می‌شود تا کلیک تکراری در همان لحظه مشکلی ایجاد نکند.
- ظاهر با کلاس‌های رنگی موجود سایت (`--turquoise`) هماهنگ است؛ هیچ بازطراحی دیگری در صفحه انجام نشد.
- هم روی دسکتاپ (Clipboard API) و هم مسیر fallback (برای موبایل/مرورگرهای قدیمی) تست شد.

---

## ۵. ساختار دسته‌بندی و زیردسته

- یک جدول واحد و خودارجاع (`categories` با `parent_id`)، نه دو جدول جدا برای دسته/زیردسته. این یعنی عمق ساختار محدود به «۲ سطح» نیست؛ در آینده دسته → زیردسته → زیر-زیردسته هم بدون تغییر جدول ممکن است.
- هر دسته: `name`, `slug` (یکتا)، `parent_id`، `sort_order`، `active`.
- محصولات فقط یک ستون nullable جدید گرفتند: `products.category_id`. هیچ محصولی حذف یا دوباره وارد نشد.

## ۶. افزودن دسته جدید در آینده از پنل

- صفحه جدید `/admin/categories/` یک فرم ساده دارد: نام، Slug (اختیاری، خودکار ساخته می‌شود)، دسته والد (یا خالی برای دسته اصلی)، ترتیب نمایش، فعال/غیرفعال.
- برای افزودن مثلاً «بخاری» یا «اجاق گاز رومیزی» در آینده: صرفاً از همین فرم استفاده می‌شود — **بدون هیچ تغییر کد یا Deploy مجدد.** این با یک تست واقعی (ایجاد دسته + زیردسته با API واقعی) تأیید شد.
- حذف دسته «امن» است: اگر دسته محصول یا زیردسته داشته باشد، حذف مستقیم رد می‌شود (پیام با تعداد دقیق) و باید یک دسته مقصد برای انتقال انتخاب شود؛ در آن صورت همه محصولات/زیردسته‌ها قبل از حذف منتقل می‌شوند. این مسیر هم با تست واقعی (ایجاد → انتساب محصول → حذف با انتقال → تأیید انتقال) تأیید شد.

---

## ۷. ساختار روابط محصولات

- یک جدول عمومی `product_relations` با ستون `relation_type` (`related` / `similar` / `complementary`). افزودن نوع رابطه جدید در آینده فقط یک مقدار متنی تازه است، نه جدول یا Migration جدید.
- مدیریت کامل از پنل محصولات: هنگام ویرایش هر محصول موجود، بخش «روابط این محصول» ظاهر می‌شود؛ جست‌وجوی زنده محصولات + افزودن/حذف با یک کلیک برای هر سه نوع رابطه، به‌صورت مستقل.
- نمایش عمومی (نوار «مرتبط»/«مشابه» در صفحه جزئیات محصول) هنوز پیاده نشده و **عمداً** به Backend محدود ماند — طبق دستور صریح شما مبنی بر خاموش‌ماندن نمایش عمومی. داده از طریق `GET /api/store/products/:slug` (فیلدهای `related_products`/`similar_products`) پشت پرچم آماده است تا هر زمان خواستید، فقط با کمی کار Frontend (بدون تغییر Backend) روشن شود.

---

## ۸. ساختار پیشنهادهای سبد خرید

- `POST /api/store/cart/suggestions` یک لیست واحد و بدون تکرار برمی‌گرداند که از دو منبع ادغام شده:
  1. روابط `complementary` تعریف‌شده دستی توسط مدیر
  2. آمار واقعی `product_co_purchases`
- این Endpoint فقط وقتی پرچم `show_cart_suggestions` روشن باشد چیزی برمی‌گرداند؛ در غیر این صورت آرایه خالی — یعنی امروز هیچ تغییری در تجربه سبد خرید مشتری دیده نمی‌شود.
- **رابط کاربری نوار «پیشنهادهای تکمیلی» در `cart.html` عمداً ساخته نشد** — چون نمایش آن طبق دستور شما باید خاموش بماند و تنها API آماده باشد؛ وقتی خواستید روشن شود، افزودن این نوار به `cart.html` یک کار Frontend کوچک و مستقل است.

## ۹. آماده‌سازی «مشتریان همراه این محصول خریده‌اند»

- جدول `product_co_purchases` یک شمارنده تجمیعی (نه Join زنده) است: `(product_id, co_product_id, times_together, last_purchased_at)`.
- هر بار یک سفارش واقعی با بیش از یک قلم ثبت شود، شمارنده هر جفت محصول داخل همان سفارش (در هر دو جهت) یک واحد اضافه می‌شود — این منطق داخل همان Handler ثبت سفارش اضافه شد و کاملاً غیربحرانی است (خطای آن هرگز سفارش را نمی‌شکند).
- با رشد تعداد سفارش‌ها، خواندن پیشنهاد برای یک محصول همیشه یک SELECT ساده و ایندکس‌شده است، نه محاسبه زنده روی کل تاریخچه سفارش‌ها.
- در پنل محصولات، هنگام ویرایش هر محصول، این لیست به‌صورت خواندنی («N بار همراه خریداری شده») نمایش داده می‌شود.
- این رفتار با یک سفارش واقعی آزمایشی (۲ قلم) تأیید شد: شمارنده دقیقاً به ۱ رسید.

---

## ۱۰. قابلیت‌هایی که فعلاً برای مشتری خاموش هستند

با پرچم‌های جدید در `site_settings` (پیش‌فرض همه `0`)، از صفحه `/admin/categories/` قابل روشن/خاموش‌کردن:

| پرچم | اثر |
|---|---|
| `show_categories_public` | (رزرو‌شده برای آینده — هنوز هیچ صفحه/منوی عمومی دسته‌بندی ساخته نشده) |
| `show_related_products` | نمایش «محصولات مرتبط» در پاسخ API صفحه محصول |
| `show_similar_products` | نمایش «محصولات مشابه» در پاسخ API صفحه محصول |
| `show_cart_suggestions` | فعال‌شدن پاسخ‌دهی endpoint پیشنهادهای سبد خرید |

هیچ‌کدام از این‌ها امروز روی صفحات عمومی (فروشگاه، جزئیات محصول، سبد خرید) قابل مشاهده نیستند، چون هیچ Frontend مشتری هنوز این فیلدها را نمی‌خواند — دقیقاً طبق دستور «نمایش عمومی خاموش، زیرساخت آماده».

---

## ۱۱. تست‌های انجام‌شده و نتیجه

تمام موارد زیر با یک هارنس واقعی روی Node.js (`node:sqlite`) و با ایمپورت مستقیم و اجرای همان کد واقعی `src/index.js` (نه شبیه‌سازی) تست شدند — ۲۵ از ۲۵ مورد موفق:

- ایجاد دسته اصلی و زیردسته با `parent_id` ✅
- رد شدن Slug تکراری ✅
- لیست دسته‌ها با `product_count` صحیح ✅
- رد دسترسی بدون توکن ادمین ✅
- انتساب دسته به محصول موجود بدون ایجاد مجدد آن ✅
- بلوکه‌شدن حذف دسته دارای وابسته (محصول/زیردسته) ✅
- حذف با انتقال صحیح محصولات/زیردسته‌ها به دسته مقصد ✅
- ایجاد/رد (self-relation)/گروه‌بندی/حذف رابطه محصول ✅
- پرچم‌ها پیش‌فرض خاموش؛ روشن‌شدن پایدار می‌ماند ✅
- فیلد `related_products` فقط بعد از روشن‌شدن پرچم در API ظاهر می‌شود ✅
- پیشنهادهای سبد خرید خالی وقتی پرچم خاموش، و ادغام صحیح منبع دستی وقتی روشن ✅
- فهرست عمومی محصولات مقدار `stock=0` را برای محصول ناموجود صحیح برمی‌گرداند (ریشه رفع باگ کارت محصول) ✅
- ثبت سفارش واقعی با موجودی کافی موفق و افزایش صحیح شمارنده خرید مشترک ✅
- رد قطعی سفارش برای محصول ناموجود، حتی با فراخوانی مستقیم API (بدون از مسیر Frontend) ✅

علاوه بر این: `node --check` روی تمام فایل‌های JS تغییریافته/جدید (`src/index.js`, `store.js`, `products.js`, `categories.js`, `admin-common.js`) و اسکریپت درون `order-success.html` اجرا و بدون خطا پاس شد.

## ۱۲. مواردی که امکان تست کامل آنها در این محیط وجود نداشت

- **رندر واقعی مرورگر** (کلیک دکمه‌ها، ظاهر بصری، رفتار Clipboard API در یک مرورگر واقعی موبایل/دسکتاپ) — این Sandbox مرورگر Headless ندارد؛ منطق JS دکمه کپی و فرم‌های ادمین از نظر نحوی (`node --check`) و منطقی (بازبینی دستی جریان کد) بررسی شدند اما رندر پیکسلی تست نشد.
- **اجرای واقعی روی Cloudflare D1/Workers** — این پروژه از قبل مستقیماً به GitHub/Cloudflare متصل نیست (فقط از طریق ZIP کار می‌شود)، پس `wrangler d1 execute`/`wrangler deploy` واقعی در این Sandbox اجرا نشد. Migration را روی یک کپی کامل و واقعی از ساختار جدول‌های فعلی (بر پایه تمام Migrationهای قبلی پروژه) در SQLite واقعی اجرا و تأیید کردم؛ اما تست نهایی روی D1 واقعی خودتان باید انجام شود.
- **حجم واقعی داده** — رفتار با ۱۰۰-۲۰۰ محصول واقعی تست نشد (چون داده واقعی در Sandbox نیست)؛ اما نمایه‌های (Index) لازم روی `products.category_id`، `product_relations` و `product_co_purchases` در Migration ایجاد شده‌اند تا با رشد داده کند نشود.
- **نمایش عمومی دسته‌بندی/مرتبط/مشابه/پیشنهادهای سبد خرید در Frontend مشتری** — طبق دستور صریح شما عمداً ساخته نشد؛ فقط Backend آن حاضر و تست شده است.

---

## نکته اجرا برای شما

قبل از استفاده از پنل دسته‌بندی/روابط محصول، حتماً این را روی D1 واقعی اجرا کنید (فقط همین یک Migration جدید است):

```bash
npx wrangler d1 execute tasisatapadana-db --remote --file=./database/categories-and-relations.sql
```

بدون اجرای این دستور، Endpointهای جدید خطای واضح «ابتدا Migration را اجرا کنید» برمی‌گردانند (نه خطای مبهم ۵۰۰)، و بقیه فروشگاه (خرید/سبد/Checkout فعلی) کاملاً دست‌نخورده و کار می‌کند.
