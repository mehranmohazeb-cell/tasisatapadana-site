# توسعه سفارش، حساب مشتری و پشتیبانی — تأسیسات آپادانا

ظاهر، طراحی و تجربه کاربری بخش‌های موجود سایت (صفحه اصلی، فروشگاه، صفحه محصول، سبد
خرید، گالری، هدر/فوتر) دست‌نخورده باقی مانده است. آنچه در این مرحله اضافه شده،
تکمیل سیستم سفارش واقعی، حساب مشتری، پیگیری مهمان و سیستم تماس/پشتیبانی است.

## ۱) پایگاه داده (D1)

دو فایل Migration مستقل:

- `database/orders-and-customers.sql`
  - ستون‌های جدید روی جدول‌های موجود `orders` و `order_items` (آدرس ساختاریافته،
    کد پیگیری، مشتری، وضعیت پرداخت، اطلاعات مرسوله پستی)
  - جداول جدید: `customers`, `customer_sessions`, `customer_addresses`,
    `order_status_history`, `sms_notifications`
- `database/support-tickets.sql`
  - جداول جدید: `tickets`, `ticket_messages`, `email_notifications`
  - ستون جدید `ticket_id` روی جدول `sms_notifications` موجود (بدون بازنویسی آن)

هر دو فایل فقط **یک‌بار** روی دیتابیس فعلی (`tasisatapadana-db`) اجرا می‌شوند؛
توضیحات کامل اجرا در ابتدای هر فایل آمده است.

نکته مهم درباره نام‌گذاری: ستون مبلغ سفارش در دیتابیس فعلی از قبل `total` نام
دارد (نه `total_amount`). برای اینکه پنل مدیریت و APIهای قبلی بدون تغییر کار
کنند، همین نام حفظ شده است.

## ۲) سفارش واقعی (Checkout)

- `POST /api/store/checkout` — دریافت اطلاعات مشتری، آدرس کامل، اعتبارسنجی،
  خواندن مجدد قیمت/موجودی از D1، کاهش اتمیک موجودی (با Rollback دستی در صورت
  شکست هر بخش)، تولید کد پیگیری یکتا (`AP-XXXXXXXXXX`)، ثبت سفارش + اقلام +
  تاریخچه وضعیت، و آماده‌سازی پیامک (صف `sms_notifications`).
- صفحه `public/store/checkout.html`: فرم کامل آدرس (استان/شهر Dropdown از
  `public/store/iran-locations.js`، کد پستی ۱۰ رقمی، موبایل، توضیحات تکمیلی)
  + خلاصه سفارش. اگر کاربر وارد حساب باشد، نام/موبایل/آدرس پیش‌فرض او پر می‌شود.
- صفحه موفقیت: `public/store/order-success.html` (کد پیگیری، مبلغ، تاریخ،
  موبایل، خلاصه اقلام).

## ۳) پیگیری سفارش مهمان

- `GET /api/store/track?tracking_code=..&mobile=..` — نیاز به هر دو مورد.
- صفحه: `public/store/track-order.html` (وضعیت، تاریخچه وضعیت، اطلاعات مرسوله
  پستی در صورت وجود).

## ۴) حساب مشتری (ثبت‌نام اختیاری؛ سفارش مهمان همچنان فعال است)

- رمز عبور با PBKDF2 (Web Crypto، بدون کتابخانه خارجی) هش می‌شود.
- ورود با نشست کوکی (`apadana_session`, HttpOnly) و جدول `customer_sessions`.
- `POST /api/store/customers/register` | `/login` | `/logout`
- `GET /api/store/customers/me` | `/address` | `/orders` | `/orders/:id`
- صفحات: `public/store/account.html` (ورود/ثبت‌نام)، `public/store/my-orders.html`

## ۵) وضعیت سفارش

`pending, confirmed, preparing, shipped, completed, cancelled` — بدون CHECK
constraint در دیتابیس، فقط نگاشت برچسب فارسی در `STATUS_LABELS` (سمت Worker) و
`ORDER_CONFIG.statuses` (سمت Frontend). افزودن وضعیت جدید در آینده (مثل
`returned`) فقط نیاز به افزودن یک خط در همین دو محل دارد.

## ۶) پرداخت (آماده برای درگاه بعدی)

ستون‌های `payment_status` (`unpaid|paid|failed|refunded`) و `payment_reference`
از هم‌اکنون در جدول `orders` وجود دارند و توسط API مدیریت سفارش قابل تغییرند.

## ۷) پنل مدیریت سفارش (تکمیل‌شده)

`public/admin/` بدون تغییر در ساختار قبلی، این موارد اضافه شد:
- نمایش کد پیگیری، آدرس کامل (ساخته‌شده از فیلدهای ساختاریافته)، وضعیت پرداخت
- فیلد ثبت شرکت پستی + کد مرسوله برای هر سفارش
- جست‌وجو/فیلتر سفارش‌ها بر اساس کد پیگیری، موبایل، نام یا وضعیت
- `GET/PUT /api/store/orders`, `/api/store/orders/:id` (توسعه‌یافته)

## ۸) SMS و ایمیل (فقط زیرساخت؛ هنوز به سرویس واقعی وصل نیست)

جدول‌های `sms_notifications` و `email_notifications` هر رویداد مهم (ثبت
سفارش، تغییر وضعیت، ثبت تیکت، پاسخ تیکت) را با وضعیت `pending` ذخیره می‌کنند.
اتصال به یک سرویس‌دهنده واقعی، فقط نیاز به یک Worker/Cron جدید دارد که ردیف‌های
`pending` را بخواند و ارسال کند؛ هیچ بازنویسی در ساختار فعلی لازم نیست. کلید
API هر سرویسی که بعداً اضافه شود، فقط در Secrets مربوط به Worker (env)
نگهداری می‌شود، نه در Frontend.

## ۹) تماس با ما و پشتیبانی (تیکت)

- صفحه `public/support.html` (سطح اصلی سایت): دکمه‌های تماس/واتساپ/ایمیل + فرم
  ثبت تیکت + فرم پیگیری تیکت. لینک آن به ناوبری صفحه اصلی، `service.html` و
  همه صفحات فروشگاه اضافه شده است.
- `POST /api/support/tickets` — ثبت تیکت (مهمان یا مشتری واردشده به حساب؛ در
  صورت ورود، `customer_id` خودکار ثبت می‌شود). کد پیگیری با پیشوند `TK-`.
- `GET /api/support/tickets/track?tracking_code=..&mobile=..`
- `GET /api/support/tickets/mine` (برای مشتری واردشده)
- وضعیت‌ها: `received (ثبت شده) → in_review (در حال بررسی) → answered (پاسخ
  داده شد) → closed (بسته شد)`.
- ساختار پیام‌ها در جدول جداگانه `ticket_messages` (چند پیام رفت‌وبرگشتی).
- ستون `order_id` روی `tickets` از هم‌اکنون برای «اتصال تیکت به سفارش» در
  آینده آماده است.
- **API مدیریت تیکت از هم‌اکنون آماده است** (طبق دستور، پنل تصویری آن در
  مرحله بعد ساخته می‌شود):
  - `GET /api/support/admin/tickets` (فهرست + جست‌وجو/فیلتر وضعیت)
  - `GET /api/support/admin/tickets/:id` (جزئیات + همه پیام‌ها)
  - `PUT /api/support/admin/tickets/:id` (تغییر وضعیت)
  - `POST /api/support/admin/tickets/:id/reply` (پاسخ مدیر؛ وضعیت خودکار به
    «پاسخ داده شد» تغییر می‌کند و پیامک/ایمیل در صف قرار می‌گیرد)
  - هر سه با هدر `X-Admin-Token` محافظت می‌شوند (همان توکن پنل فروشگاه).

## نکات امنیتی رعایت‌شده

- قیمت و موجودی همیشه از D1 خوانده می‌شود، نه از مرورگر.
- پیگیری سفارش/تیکت مهمان همیشه به **کد پیگیری + شماره موبایل** نیاز دارد.
- رمز عبور هرگز متن ساده ذخیره نمی‌شود.
- نشست کاربر با کوکی HttpOnly (نه localStorage) نگهداری می‌شود.
- APIهای مدیریت (سفارش، تیکت، محصول) همگی نیازمند `X-Admin-Token` هستند.
