# گزارش نهایی — ویرایش دوم (تکمیل سیستم ارسال + اصلاح فاکتور/گارانتی)

منبع: **فقط** آخرین ZIP تحویلی همین گفت‌وگو (`tasisatapadana-site-6tasks.zip`). هیچ نسخه قدیمی‌تری مبنا قرار نگرفت. دسته‌بندی/زیردسته‌بندی، `<base href="/store/">`، مسیرهای تمیز محصول، معماری Worker+Assets+D1+KV، ظاهر/رنگ/فونت/Header/Footer — همگی دست‌نخورده.

## ۱-۲) فایل‌های تغییرکرده / جدید

`diff -rq` نسبت به ZIP قبلی تأیید کرد **فقط** همین فایل‌ها تغییر کردند:

| فایل | تغییر |
|---|---|
| `src/index.js` | منطق برآورد ارسال، قواعد ارسال محصول، Checkout، تصویر فاکتور |
| `public/store/product.html` | بخش برآورد هزینه ارسال |
| `public/store/cart.html` | بخش برآورد هزینه ارسال (کل سبد) |
| `public/store/checkout.html` | جابه‌جایی دکمه ثبت سفارش به بعد از انتخاب ارسال؛ محاسبه سمت سرور |
| `public/store/track-order.html` | دکمه «دانلود فاکتور» |
| `public/store/store.css` | استایل حداقلی برآورد ارسال + اصلاح آیکون خراب |
| `public/admin/products/index.html`, `products.js` | بخش «قواعد ارسال این محصول» |
| `database/product-shipping-rates.sql` | **Migration جدید** |

پنج Migration قبلی (`user-management`, `order-notifications`, `technical-format-rules`, `shipping-methods`, `payment-ledger`) **دوباره اجرا نشدند** و دست‌نخورده‌اند.

## ۳) برآورد ارسال در صفحه محصول

بخش جدید «برآورد هزینه ارسال» زیر اطلاعات ارسال موجود اضافه شد: انتخاب شهر (فهرست ثابت شهرهای `iran-locations.js`، همان فایلی که Checkout استفاده می‌کند) → دریافت روش‌های مجاز همان شهر **و همان محصول** از `GET /api/store/shipping-methods?city=..&product_id=..` → نمایش هزینه تقریبی. متن تذکر دقیقاً طبق درخواست («هزینه نهایی بر اساس آدرس ثبت‌شده...») زیر آن است. هیچ‌چیز در این مرحله ذخیره/به Order متصل نمی‌شود.

## ۴) برآورد ارسال در سبد خرید

مشابه، اما روی `GET /api/store/shipping-methods?city=..&product_ids=1,2,3` (شناسه همه محصولات سبد) — هزینه ارسال کل سبد + جمع تقریبی کالا+ارسال نمایش داده می‌شود. طبق معماری فعلی سفارش، همیشه **یک** روش ارسال مشترک برای کل سبد محاسبه می‌شود؛ سیستم چند Checkout جدا نمی‌سازد.

## ۵) هزینه ارسال محصول‌محور — تصمیم معماری

**بدون سیستم موازی** — جدول جدید `product_shipping_rates` فقط جدول موجود `shipping_methods` را با یک رابطه اختیاری (محصول × روش) گسترش می‌دهد:
- بدون ردیف → هزینه پیش‌فرض همان روش ارسال (رفتار قدیم دست‌نخورده، محصولات قدیمی نیاز به ورود مجدد ندارند).
- ردیف با `custom_cost` → همان مبلغ به‌جای پیش‌فرض.
- ردیف با `is_allowed=0` → آن روش برای آن محصول اصلاً مجاز نیست.

مدیریت از بخش جدید «قواعد ارسال این محصول» در فرم ویرایش هر محصول در پنل — برای هر تعداد محصول/روش، بدون سخت‌کد.

## ۶) منطق سبد چندمحصولی (تصمیم معماری — طبق درخواست اینجا شفاف)

چون ساختار سفارش فقط **یک** `shipping_method_id`/هزینه برای کل سفارش نگه می‌دارد (نه هزینه جدا به‌ازای هر کالا)، تابع مشترک `resolveShippingOptionsForCart(env, productIds, city)` (هم برای برآورد، هم Checkout) این‌طور تصمیم می‌گیرد:
- یک روش ارسال برای کل سبد **در دسترس** است فقط اگر همه محصولات سبد آن را مجاز بدانند (اگر حتی یک محصول آن را غیرمجاز کرده باشد، آن روش کلاً از فهرست سبد حذف می‌شود).
- هزینه نهایی آن روش = **بیشترین** هزینه (اختصاصی یا پیش‌فرض) در بین محصولات سبد — نه مجموع. همان قاعده‌ای که قبل از این ویرایش هم برای ترکیب هزینه ارسال چند کالا استفاده می‌شد؛ فقط حالا هزینه هر کالا از پنل قابل تنظیم است.

این دقیقاً با تست real (پکیج+شلنگ، جایی که شلنگ «پیک اصفهان» را غیرمجاز کرده) تأیید شد: آن روش برای کل سبد ناپدید می‌شود، ولی برای سبد تک‌محصولیِ پکیج با هزینه اختصاصی‌اش در دسترس می‌ماند.

## ۷) Checkout و محاسبه نهایی سمت سرور

Checkout دیگر از یک ردیف ساده `shipping_methods` نمی‌خواند؛ دقیقاً همان `resolveShippingOptionsForCart` را با **آدرس واقعی و اقلام واقعی سبد** صدا می‌زند و روش انتخابی مشتری را با همان فهرست تطبیق می‌دهد. یعنی:
- شهر برآوردی صفحه محصول/سبد هیچ‌وقت وارد Order نمی‌شود — فقط آدرس واقعی Checkout.
- اگر مشتری در صفحه محصول «اصفهان + پیک اصفهان» دیده باشد ولی در Checkout آدرس تهران بزند، همان روش دیگر در فهرست معتبر Checkout نیست → خطای «روش ارسال ... در دسترس نیست» و مانع ثبت سفارش می‌شود. تست شد.
- ترتیب فرم Checkout: دکمه «ثبت نهایی سفارش» از داخل فرم آدرس به انتهای کارت خلاصه سفارش (بعد از انتخاب روش ارسال، هزینه، و مبلغ کل) منتقل شد — با `form="checkout-form"` هنوز به همان فرم متصل است. در موبایل (تک‌ستونه‌شدن Layout زیر ۸۶۰px، بدون تغییر در چیدمان ستون‌ها) اکنون ترتیب واقعی: مشتری/آدرس → روش ارسال → هزینه ارسال → مبلغ کل → دکمه ثبت.
- منطق COD/`payable_amount`/تبدیل تومان-ریال/Payment Ledger کاملاً دست‌نخورده ماندند؛ فقط منبع `shippingCost`/`shippingIsCod` عوض شد.

## ۸) فاکتور — تصویر واقعی محصول

**ریشه‌یابی:** هر دو Query که فاکتور را تغذیه می‌کردند (`/api/store/track` برای مهمان، `/api/store/customers/orders/:id` برای مشتری عضو) فقط `products.image` خام را بدون هیچ پیشوند مسیری برمی‌گرداندند و به گالری (`product_images`) هم مراجعه نمی‌کردند — یعنی محصولاتی که فقط عکس گالری دارند اصلاً عکس نشان نمی‌دادند. دو Endpoint پنل مدیریت هم همین مشکل را داشتند؛ هر ۴ اصلاح شدند: اول `products.image`، در نبودش اولین عکس گالری، در نهایت Placeholder — از همان تابع `resolveProductImageUrl` که بقیه سایت استفاده می‌کند (بدون منطق موازی)، به‌صورت مطلق (`resolveAbsoluteProductImageUrl`) تا در دانلود/چاپ هم درست باز شود. فرمت فایل (JPG/JPEG/WebP) برای این منطق فرقی نمی‌کند چون فقط مسیر را می‌سازد. فاکتور از صفر بازنویسی نشد — فقط منبع Query عوض شد.

## ۹) دانلود فاکتور از صفحه پیگیری

در `track-order.html`، بعد از نمایش نتیجه پیگیری، اطلاعات لازم (`tracking_code`+`mobile`) در همان `sessionStorage` که `invoice.html` (حالت مهمان) از قبل می‌خواند نوشته می‌شود، و یک دکمه «دانلود فاکتور» که همان `invoice.html` را باز می‌کند اضافه شد — بدون فاکتور جدید یا تکرار منطق. دسترسی‌های قبلی فاکتور (my-orders.html، order-success.html) دست‌نخورده ماندند.

## ۱۰) نماد خراب گارانتی/ضمانت برگشت

**منشأ:** `store.css` خط ۷۱۵، در `.product-assurance-list li::before { content: "âœ“ "; }` — یک Mojibake کلاسیک UTF-8 (بایت‌های ✓ اشتباه به‌عنوان Latin-1 ذخیره شده بودند). اصلاح شد به کاراکتر صحیح `✓`. چون این محتوا از طریق CSS `::before` تولید می‌شود، ذاتاً لینک/کلیک‌پذیر/کپی‌شدنی نیست — هیچ محدودیت اضافه‌ای لازم نبود. متن و عملکرد فیلدهای گارانتی/ضمانت برگشت دست‌نخورده. جست‌وجوی کامل پروژه، هیچ نمونه دیگری از این Mojibake پیدا نکرد.

## ۱۱) آیا Migration جدید لازم شد؟

بله — **فقط یک فایل جدید**: `database/product-shipping-rates.sql`. دلیل: ساختار قبلی `shipping_methods` هیچ رابطه‌ای با محصول نداشت؛ برای هزینه/مجاز‌بودن اختصاصی هر محصول، یک جدول رابطه‌ای (`product_shipping_rates`، FK به `products` و `shipping_methods`) لازم بود. کاملاً افزایشی — هیچ ستون/جدول موجودی تغییر نکرد؛ روی زنجیره کامل Migrationهای قبلی (شامل ۵ فایل اجراشده) با SQLite واقعی تست و تأیید شد.

```bash
npx wrangler d1 execute tasisatapadana-db --remote --file=./database/product-shipping-rates.sql
```

## ۱۲) تست‌های انجام‌شده

با همان روش قبلی (شبیه‌ساز D1 روی `node:sqlite`، صدازدن واقعی `src/index.js`): **۵۹ تست خودکار — همه موفق** (۴۷ تست رگرسیون قبلی + ۱۲ تست جدید)، شامل تمام سناریوهای اجباری بخش پنجم دستور:
- محصول با/بدون هزینه اختصاصی، شهر اصفهان/تهران، پیک اصفهان محدود به اصفهان.
- سبد چندمحصولی با هزینه‌های متفاوت → هزینه = بیشترین مقدار؛ محصولی که یک روش را غیرمجاز کرده، آن روش را از کل سبد حذف می‌کند.
- Checkout: رد سفارش وقتی روش برای یکی از کالاهای سبد مجاز نیست؛ موفقیت با هزینه اختصاصی صحیح وقتی مجاز است؛ COD/تومان-ریال هنوز صحیح (رگرسیون).
- فاکتور: تصویر JPG از `products.image`، تصویر WebP از گالری وقتی `products.image` خالی است، دو محصول تصویر متفاوت دارند (نه یک تصویر ثابت).
- بازگشت به حالت پیش‌فرض قواعد ارسال، ردیف اضافه در دیتابیس باقی نمی‌گذارد.
- رگرسیون کامل: کاربران، اعلان سفارش، Technical Format Rules، تومان/ریال، پرداخت — همه دوباره تست و موفق.

**تست نشده در این Sandbox (بدون مرورگر واقعی):** نمایش بصری Dropdown های جدید در موبایل واقعی، رندر واقعی چک‌مارک اصلاح‌شده، و کلیک دکمه «دانلود فاکتور». این‌ها فقط از نظر Syntax/منطق API تأیید شده‌اند.

## ۱۳) موارد باقی‌مانده

- بعد از Deploy، یک‌بار در مرورگر واقعی چک شود: برآورد ارسال در صفحه محصول و سبد، ترتیب جدید Checkout در موبایل، دکمه دانلود فاکتور، و ظاهر صحیح چک‌مارک گارانتی.
- قواعد ارسال محصول فعلاً فقط از فرم ویرایش تک‌تک محصولات قابل مدیریت است (نه به‌صورت گروهی/دسته‌ای) — اگر تعداد محصولات خیلی زیاد شود و ویرایش گروهی لازم شود، می‌توان بعداً یک صفحه اختصاصی «مدیریت گروهی قواعد ارسال» اضافه کرد؛ فعلاً طبق دامنه این دستور اضافه نشد.
- کار دیگری متوقف نمانده؛ همه ۱۳ بند دستور تکمیل شدند.
